spring.application.name=MyBooks

# Use PORT environment variable for Render, fallback to 8080 for local development
server.port=${PORT:8080}

# MongoDB Configuration
# Para desarrollo local
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://ettorepw:Diosesfiel1%2B@bibliotecadejunior.y2xvybm.mongodb.net/MyBookDB?retryWrites=true&w=majority&maxPoolSize=10&serverSelectionTimeoutMS=5000&connectTimeoutMS=10000&socketTimeoutMS=0}
# Base de datos
spring.data.mongodb.database=${MONGODB_DATABASE:MyBookDB}

# Spring Boot actuator (health check for Render)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
